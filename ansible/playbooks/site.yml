---
# Host variables come from inventory/group_vars/ecs_instances.yml
# (must match terraform outputs; override ad hoc with -e).
- name: Configure ECS EC2 instances
  hosts: ecs_instances
  become: true
  roles:
    - security
    - docker
    - ecs-agent
    - cloudwatch-agent
